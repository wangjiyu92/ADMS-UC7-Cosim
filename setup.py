#!/usr/bin/env python
from setuptools import setup, find_packages
from distutils.core import setup

setup(name='Co-sim',
      version='1.0',
      description='A Co-simulation of HEMS, homes, feeder and market using the energy blockchain',
      packages=find_packages(),
     )